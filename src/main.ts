import { createApp } from 'vue';
import TestimonialsDialog from '@/components/TestimonialsDialog.vue';

// Create the Vue instance.
const app = createApp({
  components: { TestimonialsDialog },
});

// Mount the application.
app.mount('#app');
